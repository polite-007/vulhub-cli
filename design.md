# vulhub-cli 设计

一个小团队共用一台 Linux 机器时，用来管理 vulhub 靶场生命周期的命令行工具。

术语定义见 [CONTEXT.md](./CONTEXT.md)，本文档不重复定义。

---

## 需求

vulhub 提供了大量开箱即用的漏洞靶场，但靶场数量太多之后，它的"管理 & 运维"能力就很薄弱：想知道有哪些靶场要翻目录，想启动一个靶场要先 `cd` 进去再敲 `docker compose`，想起完再清理又要记住它到底叫什么。逐个手工操作在靶场数量上百之后就不可行了。

需要一个命令行工具来解决这件事：查询所有靶场、启动靶场、暂停靶场、销毁靶场。

因为工具完全依赖 vulhub 这个仓库，所以还需要一个初始化命令，负责把 vulhub 拉取到本地。

## 交付物

**一个 linux-amd64 的静态二进制文件。**用户拿到它即可使用，不需要安装运行时。

## 使用场景与约束

- **小团队共用一台 Linux 机器。**多人 SSH 上去，共用同一个二进制、同一份 vulhub 检出（`~/vulhub`）。
- **只支持 linux-amd64。**不产出 macOS / Windows / ARM 版本。
- 不做多用户隔离。如果机器上有另一个人也装了这个工具、用了同一份检出，**视为同一套部署**，工具不区分调用者。此场景下产生的容器端口冲突由 Docker 自己报错，工具不做处理。
- `~/vulhub` 是**工具自己管理的只读副本**，用户不应直接修改它。

## 命令表面

`<靶场>` 参数接受**靶场编号**（`68`）或**靶场路径**（`activemq/CVE-2023-46604`），两者等价。

| 命令 | 作用对象 | 行为 |
|---|---|---|
| `init` | — | 拉取 vulhub 到 `~/vulhub`，并建立索引 |
| `ls [--json] [-time]` | — | 列出全部靶场 |
| `search <关键词>… [--json] [-time]` | — | 按漏洞标题或靶场路径检索 |
| `up <靶场>` | 单个 | 启动靶场 |
| `stop <靶场>[,…]` | 单个或多个 | 停止靶场 |
| `del <靶场>` | 单个 | 销毁靶场 |
| `status` | — | 查看当前运行中的靶场 |
| `pull <靶场>[,…]` | 单个或多个 | 拉取靶场镜像 |
| `update` | — | 更新 vulhub 检出 |
| `gen-vulfocus` | — | 重新生成 vulfocus 镜像清单（维护者用） |

**多选（逗号列表）只适用于 `pull`、`up`、`stop`。`del` 不接受多选**——它是唯一不可逆的操作，批量销毁风险过高。

### `init`

1. **前置检查**：机器上必须存在 `git` 与 `docker` 两个命令，且 `docker compose` 或 `docker-compose` 至少有一个。缺失则报错退出，明确指出缺的是哪一个。
2. 若 `~/vulhub` 已存在，**报错退出**并提示改用 `update`，绝不静默覆盖。
3. 执行 `git clone --depth 1` 拉取到 `~/vulhub`。浅克隆让体积接近 tarball，同时保留 git 元信息，使 `update` 能用 `git pull` 而非重新下载整包。
4. 克隆完成后**立即建立索引并分配全部编号**。`init` 是一个"做准备"的动作，用户预期它把事情都办妥，因此不采用惰性建立——否则"`up 68` 起的是哪个靶场"将取决于用户何时第一次运行 `ls`。

### `update`

1. 在 `~/vulhub` 执行 `git pull`。
2. **不拦截正在运行的靶场**，但拉取完成后列出受影响的运行中靶场，提示需要重启。
3. 遇到本地改动导致的冲突：**报错并原样打印 git 的输出**，工具不替用户做合并、stash 或覆盖决策。自动处理是把用户改动弄丢的最快方式。

### `ls`

每行 `编号 路径 漏洞标题`。

**默认按编号升序排列。**编号是按其引入时的路径顺序一次性分配的，所以新装的机器上这与"按路径字典序"结果完全一致。但从旧版本升级上来的机器不同：新来源的编号是**追加**的（既有编号按设计不变），而它的路径会插进原有路径中间，此时按路径显示就会让编号乱跳（1、3、4、5、2）。按编号显示对两种情况都正确。

`-time` 改为按**创建时间倒序**（新的在前）。它**只影响显示，不影响编号**——编号按路径字典序一次性分配、此后不变，这是"记住 68 号、写进脚本、口头传达给同事"能成立的前提。一旦显示顺序能改动编号，那个前提就没了。

**vulhub 来源没有创建时间**：`environments.toml` 里没有任何日期字段，所以按时间排序时它们排在最后，内部保持原有顺序。vulfocus 的时间取自 Docker Hub 的 `last_updated`（镜像推送时间），不是 `last_modified`——后者是所有仓库共享的元数据更新时间，用它排序会让所有镜像挤在同一个时间点上。

输出**分页**：先显示 20 行，之后每按一次回车显示下一屏 20 行。**输出不是 TTY 时（被管道接走、重定向到文件）自动关闭分页**，否则 `vulhub ls | grep xxx` 会挂住。

按屏而不是按行等待，有两个原因：列表动辄上千条，一行一次回车不实用；而且终端会回显用户敲下的回车（光标因此下移一格），若每行都等一次，那格空白会让每两条之间看起来都空了一行。

### `search`

- 检索字段：**`name`（漏洞标题）与 `path`（靶场路径）**。`path` 恰好包含软件名与 CVE 号，因此三类最常用的检索词——软件名、CVE 号、漏洞标题——被两个字段全覆盖。
- 多关键词之间是 **AND** 关系（`search apache rce` 要求两个词都命中）。OR 的结果集太大，没有实用价值。
- **大小写不敏感。**
- 无命中时返回**非零退出码**，便于脚本判断。
- 分页行为与 `ls` 一致，`-time` 也一样适用。

### `up`

- 后台启动（对应 `docker compose up -d`），自动拉取缺失的镜像。
- **不做端口冲突预检**，直接执行；冲突由 Docker 报错。
- 成功后**打印该靶场所有映射到宿主机的端口**，每行一个并标注来自哪个 service。多端口环境里替用户挑一个"主端口"是在猜，猜错了他还得自己去查。
- 访问地址中的 IP 取**第一块非环回、非 docker 的网卡地址**。共享机器上队友要从自己的电脑访问，需要的是内网地址而不是 `127.0.0.1`。

### `del`

- 删除该靶场的容器与网络，**默认保留 volume**。
- `-a`：额外删除该靶场 compose 文件引用到的镜像。**只删本靶场引用的镜像**，不做共享检查，也不做 `docker image prune`——镜像被其他靶场共用时删掉的后果只是下次要重新拉取，不构成数据损失；而 `prune` 会波及 vulhub 之外的用户资产，属于越界。
- **默认要求二次确认**：打印将要删除的靶场编号、路径、涉及的容器与网络，要求输入 `y`。`-y` 跳过确认。
- 不接受多选（见上）。

### `status`

- 查看**全机**运行中的容器，但**按靶场分组**展示：每个运行中的靶场一个块，块内列出它的容器与端口。用户脑子里想的是"我起了哪几个靶场"，不是"我起了哪几个容器"。
- `--json` 输出结构化数据，供脚本与监控使用。**这是先加便宜、后加昂贵的能力**——一旦同事把 `status` 写进脚本，事后补上会破坏已有解析逻辑，因此一期就做。

### `pull`

- 拉取指定靶场的镜像。
- `--all` 拉取全部靶场的镜像。**必须显式指定**：靶场镜像普遍 100MB~1GB，全量是几十 GB 级别，默认触发是灾难。它的真实用途是"要断网演练了，先把要用的都拉下来"。

## 关键机制

### 靶场的寻址：路径是身份，编号是别名

**靶场路径是唯一身份标识，靶场编号只是它的别名。**两者在命令中都可使用。

这个主从关系是刻意的。编号是"位置"而非"身份"：它是工具自己发出去的，vulhub 上游并不知道它。一旦索引损坏或被重建，编号会重新分配——此时用户仍然可以用路径精确表达自己想要的靶场，工具不会因为"编号乱了"而不可用。

**同一个靶场最多只有一份运行中的实例。**一份 vulhub 检出对应一套固定的 compose 项目名，因此不存在"同一靶场起两份"这种情况。

### 索引

索引是靶场路径与编号之间的持久映射，**是工具唯一需要持久保存的状态**。

- **位置**：`~/.local/share/vulhub-cli/index.json`。
  **必须放在 `~/vulhub` 之外**——`~/vulhub` 是 git 仓库，把索引写进去会产生未跟踪文件，使 `update` 的 `git pull` 面对脏工作区。
- **分配顺序**：按靶场路径字典序，在 `init` 时一次性分配。
  选字典序是因为它完全由本地目录决定、可复现：万一索引丢失需要重建，只要检出内容没变，还原出的编号基本一致，用户记住的编号不会全部作废。
- **不变性**：编号一旦分配就不随 vulhub 更新而改变。`update` 带来的新靶场**追加到末尾**；消失的靶场其编号**作废、不复用**。
- **路径失效**：索引中的路径在 `~/vulhub` 里不存在时**静默跳过**，不报错。`update` 删除靶场是正常事件，每次都报错会变成噪音。
- **索引丢失或损坏**：**自动按当前情况重建，但必须打印醒目警告**，说明编号已重新分配、此前记住的编号不再有效。这是编号方案固有的弱点——索引是唯一真相源，丢了编号就全乱。上面"路径是身份"的设计正是为了给这种情况兜底。

### 来源

靶场有**两个来源**，用靶场路径的前缀区分：`vulfocus/` 开头的是 vulfocus 来源，其余是 vulhub 来源。

**vulhub 来源**就是检出里的一个目录，`docker-compose.yml` 由上游提供，我们只读。

**vulfocus 来源**来自 Docker Hub 的 `vulfocus` 命名空间。上游只提供镜像，**没有 compose 文件、也没有任何启动说明**，所以环境定义由我们**合成**：

```yaml
services:
  target:
    image: vulfocus/drupal-cve_2018_7600
    ports: ["80:80", "443:443"]
```

合成文件写在 `~/.local/share/vulhub-cli/vulfocus/<镜像名>/docker-compose.yml`，**第一次 `up` 该靶场时才生成**。它是纯派生数据，丢了可以从镜像清单重新生成。

这条设计的关键收益是：**"靶场 = compose 项目"这个不变式没有破**。`up` / `stop` / `del` / `status` / `pull` 全部不需要知道来源。

**端口**取自镜像自带的 `ExposedPorts`，**全部发布，宿主端口 = 容器端口**。镜像不需要额外的启动命令——它们自带 `Entrypoint`/`Cmd`，会自己把服务起起来。多发布几个非 web 端口（jenkins 的 50000、tomcat 的 8009）是无害的：`up` 本来就会把所有映射端口打出来。

### vulfocus 镜像清单

vulfocus 的端口各不相同（实测：`80+443`、`8080`、`8090`、`6379`、`7001+5556`、`50000+8080`），必须逐张获取。而获取它需要对每个镜像请求三次（列表、manifest、config blob，其中 config 只有约 12KB，不需要拉整个镜像），452 个镜像合计约一千四百次请求。

**这份清单由项目在发布前生成、内嵌进二进制**，而不是让每个用户去拉。理由与取舍见 [ADR 0002](docs/adr/0002-embedded-vulfocus-catalog.md)。

- 生成：`vulhub gen-vulfocus`，产物写入 `internal/vulfocus/data/vulfocus.json` 并提交进仓库
- 分发：`go:embed` 打进二进制，用户 `init` 时**零 Docker Hub 网络**
- 更新：`update` **只管 `~/vulhub` 的 `git pull`，完全不碰 Docker Hub**。刷新清单是项目侧的动作，随新版本发布

**生成器需要 Docker Hub 凭证。**Docker Hub 对匿名请求限制翻页（offset 到 100 就拒绝），而命名空间里有 452 个镜像，匿名只能取到前 100 个。因此：

```sh
DOCKERHUB_USERNAME=<用户名> DOCKERHUB_TOKEN=<PAT> vulhub gen-vulfocus
```

PAT 权限 Read-only 即可。生成器**宁可失败也不发半份清单**——取到一半翻不动时直接报错，而不是悄悄产出只有 100 个靶场的清单。单个镜像不可用（比如没有 `latest` 标签，`docker pull` 同样拉不动）不会拖垮整轮，但会被逐个列出来。

`ls` 显示 vulfocus 靶场的**标题由镜像名推导**：`drupal-cve_2018_7600` → `Drupal CVE-2018-7600`。Docker Hub 的 description 字段基本是空的，够不到更好的来源；纯字符串变换没有猜测成分，却让**按 CVE 号检索变得可用**——镜像名里是 `cve_2018_7600` 这种下划线形式，不推导的话搜 `CVE-2018-7600` 会零命中。

### 数据来源

`ls` 与 `search` 的元数据**优先解析 `~/vulhub/environments.toml`**。它是 vulhub 官方的环境注册表，含 `name` / `cve` / `app` / `path` / `tags` / `dockerfile` 六个字段，且由仓库的 CI 校验其与真实目录一致。

**该文件缺失或解析失败时，降级为扫描目录树**，此时只能得到靶场路径，`ls` 与 `search` 的标题列为空。这条降级路径的存在，是因为 `environments.toml` 在 vulhub 主分支上的存在性未能最终确认（见"未验证项"）。

vulfocus 来源的靶场清单**不来自检出**，而来自上述内嵌的镜像清单。两者合并后构成完整的靶场清单。

### 靶场识别
`status` 判断"某个容器属于哪个靶场"，靠 **compose 自动打上的标签**：

- `com.docker.compose.project` — 项目名
- `com.docker.compose.project.working_dir` — compose 文件所在的绝对路径，砍掉 `~/vulhub/` 前缀即为靶场路径

**不靠容器名反推。**官方 vulhub 的 compose 文件基本不写 `container_name`，容器名实际是 compose 自动生成的 `<项目名>-<service>-<序号>`；解析这个格式等于依赖 compose 的实现细节，换版本就可能失效，而且用户手工 `docker run` 造的容器会污染解析。

**代价**：`status` 只能看见由 compose 创建的容器。队友手工 `docker run` 起的容器不会出现。在 vulhub 的使用场景下这个边界是合理的。

**一个靶场对应一个 compose 项目，无论它内含几个容器。**vulhub 中既有单容器环境，也有"应用 + 数据库"这样的多容器环境；`up` / `stop` / `del` 一律作用在整个项目上，单容器情形是 N=1 的退化，代码无需特判。

### 调用 docker compose

工具通过**调用 `docker compose` 命令行**来操作，不使用 Docker SDK。

`init` 的前置检查允许 `docker compose`（v2，Docker 子命令）或 `docker-compose`（v1，独立命令）二者其一存在，因此工具需要在运行期探测并使用可用的那一个。

## 非目标

明确不做的事，与要做的事同等重要：

- **多用户隔离与权限**。不区分调用者，不做"只能操作自己创建的靶场"。
- **端口冲突预检**。交给 Docker 报错。
- **镜像清理**。不做 `docker image prune`，不做跨靶场的镜像共享检查。
- **交互式选择器**。`up` 只接受显式参数，不支持模糊匹配或方向键选择。`search` 负责查找。
- **`info` / `logs` / `exec` 命令**。一期不做。用户需要时可以自行 `docker compose logs`。
- **`ls` / `search` 的字段限定**（如 `--cve`、`--tag`）。等实际使用中觉得吵了再加。
- **非 linux-amd64 平台。**
- **配置文件。**vulhub 检出位置硬编码为 `~/vulhub`。

## 未验证项

以下事实**未被直接证实**，实现前应确认。每项都已在设计中留有退路。

| 项 | 影响 | 退路 |
|---|---|---|
| `environments.toml` 是否存在于主分支 | 决定 `ls`/`search` 能否显示标题、能否按 CVE 搜 | 降级为扫描目录树 |
| vulhub 的靶场总数（量级判断为千级） | 影响 `ls` 的可读性 | 已用分页缓解 |
| 是否每个靶场都恰好一个 `docker-compose.yml` | 影响 `up`/`pull` 的实现 | 若有环境含多个 compose 文件，需明确取舍规则 |
| vulhub 仓库的克隆体积 | 影响 `init` 的耗时预期与是否需要进度提示 | 已选 `--depth 1` 压低体积 |

以下事实**已核实**，记录在此避免重复怀疑：

- `environments.toml` 采用 `[[environment]]` 数组格式，字段为 `name` / `cve` / `app` / `path` / `tags` / `dockerfile`。
- vulhub 的 compose 文件**写死宿主端口**（形如 `"8080:80"`），因此 `up` 打印访问地址是可行的。
- **多 service 环境确实存在**（如 Joomla CVE-2015-8562 为 web + mysql）。
- **官方 vulhub 的 compose 文件基本不写 `container_name`。**
