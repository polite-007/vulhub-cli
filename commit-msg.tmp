生成器改为增量刷新，并区分"不可用"与"没取到"

刷新 vulfocus 清单时撞上限流，暴露出三个问题：

1. 全量重拉必然失败。Docker Hub 对认证账号的限额是每小时 200 次拉取
   （ratelimit-limit: 200;w=3600），而命名空间有四百多个镜像。实测三轮
   都在 380 个左右断掉。现在读入上一次的清单，只重拉上游确实变了的镜像
   ——镜像的端口只在被重新推送时才变，推送时间会告诉我们这件事。增量后
   通常只需拉几十个。

2. 守卫把两种性质完全不同的跳过混为一谈。404（没有 latest 标签）是这个
   镜像本来就跑不起来，属合法跳过；429 与网络中断是我们没能查明，属失败。
   原先按总数算比例，于是 100 个跳过里 81 个其实可恢复，却和 16 个真正
   不可用的被同等对待。现在分开计数，只有"没取到"才拦整轮。
   另加兜底：结果比上次缩水两成以上判失败，防凭证失效这类整体故障。

3. 失败时不说明原因。跳过名单只在成功路径打印，守卫触发时只留下一句
   "有 N 个取不到"。这个缺陷导致我一度误判失败原因（在空日志里 grep 429
   得出"零个 429"的结论）。现在无论成败都逐个报出，并按性质分组。

ADR 0002 记录限额与增量设计的由来。

Co-Authored-By: Claude Code <noreply@anthropic.com>
